var comment = document.querySelector(".containerComment")
var all_comment = document.querySelector('[data-answer-after="<%= @answer.id %>"]')

comment.parentNode.removeChild(comment)

all_comment.insertAdjacentHTML("afterend", "<%= j render partial: "shared/add_comment" %>")


